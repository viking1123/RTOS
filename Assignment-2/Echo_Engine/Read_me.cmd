Instructions to run:

Server:

gcc -o server server.c -lpthread
./server
Client:

gcc -o c1 c1.c // Similarly create executables for other clients
./c1  // When program runs on single system, run other executables too
